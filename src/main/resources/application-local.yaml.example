# 이 파일을 application-local.yaml 로 복사한 뒤 실제 값으로 채우세요.
#   cp application-local.yaml.example application-local.yaml
# application-local.yaml 은 .gitignore 에 등록되어 커밋되지 않습니다.
spring:
  datasource:
    password: your-db-password
jwt:
  secret: your-jwt-secret-key
